"use client";

import { ChangeEvent, FormEvent, useEffect, useState } from "react";
import { getSupabaseBrowserClient } from "@/lib/supabase-browser";
import { uploadProductImage } from "@/lib/product-image-upload";

type Product={id:string;name:string;slug:string;description:string|null;price:number;compare_at_price:number|null;sku:string|null;stock:number;sizes:string[];image_url:string|null;is_active:boolean;category_id:string|null;seo_title:string|null;seo_description:string|null;material:string|null;care_instructions:string|null;featured:boolean};
type Category={id:string;name:string};
type GalleryImage={id:string;product_id:string;image_url:string;alt_text:string|null;display_order:number;is_primary:boolean};
const blank={name:"",slug:"",description:"",price:"",compare:"",sku:"",stock:"0",sizes:"",category:"",image:"",active:true,seoTitle:"",seoDescription:"",material:"",care:"",featured:false};

export default function ProductsAdminPage(){
 const [products,setProducts]=useState<Product[]>([]),[categories,setCategories]=useState<Category[]>([]),[form,setForm]=useState(blank),[editing,setEditing]=useState<string|null>(null),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[uploading,setUploading]=useState(false),[message,setMessage]=useState("");
 async function load(){setLoading(true);const s=getSupabaseBrowserClient();if(!s){setMessage("SUPABASE ENVIRONMENT VARIABLES ARE NOT CONFIGURED.");setLoading(false);return;}const [{data:p,error:pe},{data:c}]=await Promise.all([s.from("products").select("id,name,slug,description,price,compare_at_price,sku,stock,sizes,image_url,is_active,category_id,seo_title,seo_description,material,care_instructions,featured").order("created_at",{ascending:false}),s.from("categories").select("id,name").order("display_order",{ascending:true})]);if(pe)setMessage(pe.message.toUpperCase());else setProducts((p as Product[])||[]);setCategories((c as Category[])||[]);setLoading(false);}
 useEffect(()=>{load()},[]);
 function change(key:keyof typeof blank,value:string|boolean){setForm(f=>({...f,[key]:value}));}
 function slugify(v:string){return v.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");}
 function edit(p:Product){setEditing(p.id);setForm({name:p.name,slug:p.slug,description:p.description||"",price:String(p.price),compare:p.compare_at_price==null?"":String(p.compare_at_price),sku:p.sku||"",stock:String(p.stock),sizes:(p.sizes||[]).join(", "),category:p.category_id||"",image:p.image_url||"",active:p.is_active,seoTitle:p.seo_title||"",seoDescription:p.seo_description||"",material:p.material||"",care:p.care_instructions||"",featured:p.featured});window.scrollTo({top:0,behavior:"smooth"});}
 function reset(){setEditing(null);setForm(blank);}
 async function handleImage(e:ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;setUploading(true);setMessage("");const s=getSupabaseBrowserClient();if(!s){setMessage("SUPABASE ENVIRONMENT VARIABLES ARE NOT CONFIGURED.");setUploading(false);return;}try{const url=await uploadProductImage(s,file,form.slug.trim()||slugify(form.name)||"product");change("image",url);setMessage("IMAGE UPLOADED.");}catch(error){setMessage(error instanceof Error?error.message.toUpperCase():"IMAGE UPLOAD FAILED.")}finally{setUploading(false);e.target.value=""}}
 async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setMessage("");const s=getSupabaseBrowserClient();if(!s){setMessage("SUPABASE ENVIRONMENT VARIABLES ARE NOT CONFIGURED.");setSaving(false);return;}const name=form.name.trim(),slug=form.slug.trim()||slugify(name),price=Number(form.price),compare=form.compare.trim()?Number(form.compare):null,stock=Number(form.stock);if(!name||!slug){setMessage("PRODUCT NAME IS REQUIRED.");setSaving(false);return;}if(!Number.isFinite(price)||price<0){setMessage("PRICE MUST BE A VALID NON-NEGATIVE NUMBER.");setSaving(false);return;}if(compare!==null&&(!Number.isFinite(compare)||compare<price)){setMessage("COMPARE PRICE MUST BE VALID AND NOT LOWER THAN PRICE.");setSaving(false);return;}if(!Number.isFinite(stock)||stock<0){setMessage("STOCK MUST BE A VALID NON-NEGATIVE NUMBER.");setSaving(false);return;}const payload={name,slug,description:form.description.trim()||null,price,compare_at_price:compare,sku:form.sku.trim()||null,stock:Math.floor(stock),sizes:form.sizes.split(",").map(x=>x.trim()).filter(Boolean),image_url:form.image.trim()||null,category_id:form.category||null,is_active:Boolean(form.active),seo_title:form.seoTitle.trim()||null,seo_description:form.seoDescription.trim()||null,material:form.material.trim()||null,care_instructions:form.care.trim()||null,featured:Boolean(form.featured),updated_at:new Date().toISOString()};const result=editing?await s.from("products").update(payload).eq("id",editing):await s.from("products").insert(payload);if(result.error)setMessage(result.error.message.toUpperCase());else{setMessage(editing?"PRODUCT UPDATED.":"PRODUCT CREATED.");reset();await load();}setSaving(false);}
 async function toggle(p:Product){const s=getSupabaseBrowserClient();if(!s)return;const{error}=await s.from("products").update({is_active:!p.is_active,updated_at:new Date().toISOString()}).eq("id",p.id);if(error)setMessage(error.message.toUpperCase());else load();}
 async function remove(p:Product){if(!window.confirm(`Delete ${p.name}?`))return;const s=getSupabaseBrowserClient();if(!s)return;const{error}=await s.from("products").delete().eq("id",p.id);if(error)setMessage(error.message.toUpperCase());else load();}
 return <div className="admin-dashboard"><header className="admin-header"><div><span>02 — CATALOG</span><h1>PRODUCT<br/><i>ROOM.</i></h1></div><div className="admin-status"><span className="status-dot"/> SUPABASE / LIVE<br/><small>PRODUCT CRUD + METADATA</small></div></header><section className="admin-section"><div className="admin-section-title"><span>{editing?"EDIT PRODUCT":"ADD PRODUCT"}</span><span>CATALOG / CORE</span></div><form className="admin-form" onSubmit={save}><label>Product name<input required value={form.name} onChange={e=>change("name",e.target.value)} placeholder="Gulab Silk Saree"/></label><label>Slug<input value={form.slug} onChange={e=>change("slug",e.target.value)} placeholder="gulab-silk-saree"/></label><label>Category<select value={form.category} onChange={e=>change("category",e.target.value)}><option value="">Select category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Description<textarea value={form.description} onChange={e=>change("description",e.target.value)} rows={3}/></label><div className="form-row"><label>Price<input required type="number" min="0" step="0.01" value={form.price} onChange={e=>change("price",e.target.value)}/></label><label>Compare price<input type="number" min="0" step="0.01" value={form.compare} onChange={e=>change("compare",e.target.value)}/></label></div><div className="form-row"><label>SKU<input value={form.sku} onChange={e=>change("sku",e.target.value)}/></label><label>Stock<input type="number" min="0" step="1" value={form.stock} onChange={e=>change("stock",e.target.value)}/></label></div><label>Sizes <small>comma separated</small><input value={form.sizes} onChange={e=>change("sizes",e.target.value)} placeholder="Free Size, 36, 38"/></label><label>Material<input value={form.material} onChange={e=>change("material",e.target.value)} placeholder="Pure cotton / silk blend"/></label><label>Care instructions<textarea value={form.care} onChange={e=>change("care",e.target.value)} rows={3} placeholder="Dry clean recommended"/></label><label>SEO title<input value={form.seoTitle} onChange={e=>change("seoTitle",e.target.value)} maxLength={70}/></label><label>SEO description<textarea value={form.seoDescription} onChange={e=>change("seoDescription",e.target.value)} rows={3} maxLength={160}/></label><label>Image URL<input type="url" value={form.image} onChange={e=>change("image",e.target.value)} placeholder="https://..."/></label><label>Upload product image<input type="file" accept="image/jpeg,image/png,image/webp" onChange={handleImage} disabled={uploading}/><small>{uploading?"UPLOADING IMAGE…":"JPG, PNG or WEBP · MAX 5MB"}</small></label>{form.image&&<div className="admin-image-preview"><img src={form.image} alt="Product preview"/><button type="button" className="table-action" onClick={()=>change("image","")}>REMOVE IMAGE</button></div>}<label className="check-row"><input type="checkbox" checked={form.featured} onChange={e=>change("featured",e.target.checked)}/> Featured product</label><label className="check-row"><input type="checkbox" checked={form.active} onChange={e=>change("active",e.target.checked)}/> Active on storefront</label><div className="form-actions"><button className="admin-action admin-submit" disabled={saving||uploading}>{saving?"SAVING…":editing?"UPDATE PRODUCT →":"CREATE PRODUCT →"}</button>{editing&&<button type="button" className="admin-action" onClick={reset}>CANCEL</button>}</div></form>{message&&<div className="admin-empty">{message}</div>}</section><section className="admin-section"><div className="admin-section-title"><span>PRODUCTS / {products.length}</span><button className="admin-action" onClick={load}>REFRESH ↻</button></div>{loading?<div className="admin-empty">LOADING PRODUCTS…</div>:products.length===0?<div className="admin-empty">NO PRODUCTS YET.</div>:<div className="admin-table"><div className="admin-row admin-row-head"><span>PRODUCT</span><span>SKU</span><span>PRICE</span><span>STOCK</span><span>ACTION</span></div>{products.map(p=><div className="admin-row" key={p.id}><strong>{p.name}</strong><span>{p.sku||"—"}</span><span>₹ {Number(p.price).toLocaleString("en-IN")}</span><span>{p.stock}</span><span className="row-actions"><button className="table-action" onClick={()=>edit(p)}>EDIT</button><button className="table-action" onClick={()=>toggle(p)}>{p.is_active?"HIDE":"SHOW"}</button><button className="table-action danger" onClick={()=>remove(p)}>DELETE</button></span></div>)}</div>}</section></div>;
}
